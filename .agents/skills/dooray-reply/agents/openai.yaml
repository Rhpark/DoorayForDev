interface:
  display_name: "Dooray 업무 댓글"
  short_description: "Dooray 업무에 지정한 댓글과 실제 사용자 멘션을 안전하게 등록합니다"
  default_prompt: "Use $dooray-reply to add the exact requested comment to a Dooray task."

policy:
  allow_implicit_invocation: false
