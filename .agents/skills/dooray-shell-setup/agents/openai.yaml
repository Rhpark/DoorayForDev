interface:
  display_name: "Dooray 셸 명령 설정"
  short_description: "PowerShell 프로필에 Dooray 단축 명령을 안전하게 등록합니다"
  default_prompt: "Use $dooray-shell-setup to register Dooray shortcuts in my PowerShell profile."

policy:
  allow_implicit_invocation: false
