---
name: dooray-report
description: Dooray 이력을 읽고 분석해, 이 프로젝트 코드에 어떻게 적용할지 방안을 REPORT.md로 작성하거나 갱신하고 화면에 보여준다. 현황 요약이 아니라 적용 방안까지 담는다. 구현은 하지 않는다. 사용법 /dooray-report <이력번호>. 사용자가 이 명령을 직접 호출했을 때 사용.
disable-model-invocation: true
model: opus
effort: high
allowed-tools:
  - Read
  - Grep
  - Glob
  - Write(Dooray/report/**)
  - Edit(Dooray/report/**)
  - PowerShell(python Dooray/dooray.py full *)
  - PowerShell(python Dooray/dooray.py download *)
  - Bash(python Dooray/dooray.py full *)
  - Bash(python Dooray/dooray.py download *)
  - PowerShell(Get-Date *)
---

목적: 이력을 이해하고, 이 저장소 코드 기준으로 "어떻게 적용/해결할지" 방안을 문서화하고 화면에도 보여준다. **구현은 절대 하지 않는다 — 문서 작성·갱신과 화면 전달까지가 이 skill의 전부다.**

이 skill을 사용자가 직접 호출한 것은 다음 두 파일 작업만 승인한 것으로 본다.

- 분석에 필요한 첨부파일을 `Dooray/report/<이력번호>/download/`에 저장
- 보고서 `Dooray/report/<이력번호>/REPORT.md`를 생성하거나 갱신

그 외 소스 코드·설정 파일 수정, Git 커밋, Dooray 댓글 등록·상태 변경 등 실제 변경은 금지한다.

**사실 기반 원칙 (문서 전체 관통):** 이력 본문·댓글·첨부파일·실제 저장소 코드에서 **확인한 사실만** 쓴다. 추측·상상·"아마 이럴 것이다"는 금지. 확인되지 않은 것은 지어내지 말고 "열린 질문 / 확인 필요"(사람이 답을 정해줘야 하는 것) 또는 "리스크 / 미확인 사항"(확인 못 했거나 위험한 사실)으로 분류해 넘긴다.

1. 저장소 루트로 이동한 뒤 이력 전체(본문 + 댓글)를 읽는다:

```
python Dooray/dooray.py full <이력번호>
```

진짜 요구사항이 댓글에 있는 경우가 많으므로 `read`가 아니라 `full`을 쓴다. `full` 원본은 분석 자료로만 사용하고 화면에 출력하지 않는다. `full` 출력에는 태그(예: BUG/ADD/CHANGE/DELETE)도 포함되는데, 태그가 있으면 **작업 성격(버그 수정인지 신규 추가인지 개선인지 등)을 파악하는 근거로 분석·방안에 반영한다.**

1-1. 첨부파일이 있고 분석에 도움이 될 것 같으면 다운로드해서 직접 읽는다:

```
python Dooray/dooray.py download <이력번호> <파일명>
```

- 받은 파일은 `Dooray/report/<이력번호>/download/<파일명>`에 저장되므로, 그 경로를 Read로 읽는다. **읽어서 확인한 내용은 보고서 "분석" 섹션에 "어느 첨부에서 무엇을 확인했는지" 근거로 명시한다**(예: "`설계서.pdf`에서 응답 포맷이 X로 규정됨"). 받아놓고 문서에 근거를 안 남기지 않는다.
- **직접 분석 가능한 형식만 받는다**: `.md`, `.csv`, `.json`, `.png`, `.jpg`/`.jpeg`, `.pdf`. 이 형식들은 Read 도구로 내용을 읽을 수 있다.
- **그 외 형식(예: `.pptx`/`.docx`/`.xlsx`)은 Read로 내용을 못 읽으므로 다운로드/분석하지 말고**, 3번의 "리스크 / 미확인 사항"에 "첨부 `<파일명>` 내용 미확인(분석 불가 형식)"으로 명시한다.

2. 보고서 경로를 `Dooray/report/<이력번호>/REPORT.md`로 정하고, 기존 보고서가 있으면 먼저 읽는다. 그다음 이력이 요구하는 바를 파악하고 이 프로젝트 코드에서 관련 부분을 탐색한다. 탐색 범위는 다음 규칙으로 정한다:

- **시작점**: 이력·댓글에서 뽑은 키워드로 Grep/Glob 검색 → 매칭된 정의로 따라 들어간다. 코드베이스를 처음부터 통독하지 않는다.
- **중단 기준**: 이력이 요구하는 **각 항목마다** (a) 관련 코드를 찾으면 근거를 달고, (b) 못 찾으면 "관련 코드 없음"이라고 명시한 뒤 멈춘다. 모든 요구 항목이 근거 또는 '없음' 판정을 받으면 탐색 종료 — 요구사항과 무관한 영역은 훑지 않는다.
- **규모 상한**: 이력이 여러 영역에 걸쳐 크면 전부 상세히 매핑하려 하지 말고, 영향받는 영역을 상위 수준으로만 짚고 상세 매핑은 파이프라인(아래 3번)으로 넘긴다.

**방안에 등장하는 코드는 반드시 실제로 확인한 것만 근거로 댄다 — `파일경로`의 `심볼명(인자)` 형식으로(예: `Dooray/dooray.py`의 `resolve_workflow(name)`). 함수가 아닌 것(설정 키·상수·CLI 하위명령 등)은 그 이름을 그대로 쓴다(예: `Config.md`의 `COMPANY=`). 줄번호는 편집 시 금방 어긋나므로 쓰지 않는다. 있을 법한 파일명이나 코드를 지어내지 않는다.**

3. 기존 보고서가 있으면 최신 Dooray 본문·댓글과 현재 저장소 코드를 기준으로 갱신하고, 없으면 새로 작성한다. 보고서에는 아래 내용을 한국어로 작성한다:

- **문서 정보**: 이력번호 / 최초 작성 시각 / 최종 갱신 시각
- **이력 요약**: 번호 / 제목 / 현재 상태 / 태그 / 핵심 요구사항
- **분석**: 무엇을 해야 하는가, 왜 (이력·댓글 근거). 첨부파일을 읽었다면 **어느 첨부에서 무엇을 확인했는지**도 여기에 근거로 포함
- **이 프로젝트에서의 적용 방안**: 건드릴 영역·파일, 접근 순서 — 각 항목에 실제 확인한 `파일경로`의 `심볼명(인자)` 근거 포함
- **열린 질문 / 확인 필요**: 사실만으로 확정 못 해 사람이 답을 정해줘야 하는 것. 각 질문 밑에 `→ 답변:` 빈 자리를 둬서, 이어받는 사람이 채우고 갱신 이력에 남길 수 있게 한다. 없으면 "없음"
- **리스크 / 미확인 사항**: 첨부파일 내용 미확인(분석 불가 형식), 코드에서 근거를 못 찾은 부분 등 확인 못 했거나 위험한 사실
- **갱신 이력**: 갱신 시각 / 이전 결론에서 변경하거나 제거한 내용과 그 이유 (열린 질문에 답이 채워진 것도 여기 기록)
- 규모가 큰 작업이면: "실제 구현은 `/grill-me` → `/to-spec` → `/implement` 파이프라인으로 진행하라"고 안내 (이 보고서를 그 입력 자료로 쓸 수 있음)

기존 보고서를 갱신할 때는 새 보고서를 뒤에 통째로 덧붙이지 않는다. 본문을 최신 분석으로 고치되 최초 작성 시각과 기존 갱신 이력은 유지한다. 기존 결론을 변경하거나 제거하면 그 이유를 새 갱신 이력으로 남긴다.

4. 현재 시각을 구한다(PowerShell: `Get-Date -Format "yyyy-MM-dd HH:mm"`). 폴더가 없으면 만든 뒤 보고서를 다음 경로에 저장한다:

```
Dooray/report/<이력번호>/REPORT.md
```

처음 작성하면 최초 작성 시각과 최종 갱신 시각을 현재 시각으로 기록한다. 기존 보고서를 갱신하면 최초 작성 시각은 유지하고 최종 갱신 시각만 현재 시각으로 바꾼다. 같은 폴더의 `download/`에는 위 1-1에서 받은 첨부파일이 들어간다. `REPORT.md`는 Git으로 추적되어 다른 사람이 이어받을 때 참고할 수 있다.

5. 저장한 파일 경로를 알려주고, **저장한 문서 내용을 그대로 화면에도 출력한다**(요약하지 않는다).
